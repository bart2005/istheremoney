{
  "name": "isthere.money",
  "private": true,
  "version": "0.0.0",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build && cp src/_redirects dist/",
    "build-dev": "parcel build --no-cache"
  },
  "devDependencies": {
    "@parcel/packager-xml": "^2.16.3",
    "@parcel/transformer-xml": "^2.16.3",
    "parcel": "^2.16.2"
  }
}
